#Having comments makes errors easier to fix.

#Commands are read in interactive mode.

#Primary prompt uses (>>>) while secondary prompts use (...).

#Integer numbers use (int) while fractions use (float).

#Class tags need to be activated using the "class" tag or have a variable using "MyClass".

#Making looping dictionaries is almost the same as using items().

#You can make a loop for more than two sequences by using the zip() function.

#The range function is used to generate arithmetic progressions.
